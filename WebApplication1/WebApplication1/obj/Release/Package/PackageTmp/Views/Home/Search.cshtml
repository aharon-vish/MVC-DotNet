@model IEnumerable<WebApplication1.Models.Store>

@*@{
        ViewBag.Title = "Search";
        Layout = "~/Views/Shared/_Layout.cshtml";
    }*@

<h2>Search</h2>

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@
<div class="container" style="background-color:whitesmoke;border-radius: 25px;">
    <table class="table">
        <tr>
            <th style="text-align:center">
                <b style="color:seagreen">Store</b>
            </th>
            <th style="text-align:center">
                <b style="color:seagreen">Address</b>
            </th>
            <th style="text-align:center">
                <b style="color:seagreen">Opening hours</b>
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr class="details">
                <td>
                    <b>  @Html.DisplayFor(modelItem => item.NameOfStroe)</b>
                </td>

                <td>
                    <b>  @Html.DisplayFor(modelItem => item.ContactUs)</b>
                </td>
                <td>
                    <b>   @Html.DisplayFor(modelItem => item.StoreHours)</b>
                </td>
                @*<td>
                        @Html.ActionLink("Edit", "Edit", new { id=item.StoreID }) |
                        @Html.ActionLink("Details", "Details", new { id=item.StoreID }) |
                        @Html.ActionLink("Delete", "Delete", new { id=item.StoreID })
                    </td>*@
            </tr>
        }

    </table>
</div>
<script>
    $(".details:even").css('background-color', '#E8FFF0');
</script>
